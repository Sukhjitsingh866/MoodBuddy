import { SafeAreaView,Text,View } from "react-native";

export default function Achievements() {
    return (
        <SafeAreaView>
        <Text> This page shows  the achievements. working text in the screen</Text>
        </SafeAreaView>
    );
}