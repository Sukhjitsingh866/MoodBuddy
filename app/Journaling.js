import { SafeAreaView,Text,View } from "react-native";

export default function Journaling() {
    return (
        <SafeAreaView>
        <Text> This is the page where you write your journal. working text in the screen</Text>
        </SafeAreaView>
    );
}