import { SafeAreaView,Text,View } from "react-native";

export default function ChatBot() {
    return (
        <SafeAreaView>
        <Text> This page is for talking to chatgpt. working text in the screen</Text>
        </SafeAreaView>
    );
}