import { SafeAreaView,Text,View } from "react-native";

export default function HabitBuilder() {
    return (
        <SafeAreaView>
        <Text> This is the page that has the habit builder in it . working text in the screen</Text>
        </SafeAreaView>
    );
}