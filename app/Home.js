import { SafeAreaView,Text,View } from "react-native";

export default function Index() {
    return (
        <SafeAreaView>
        <Text> This is the home page. working text in the screen</Text>
        </SafeAreaView>
    );
}